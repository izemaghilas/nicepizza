{% extends 'base.html.twig' %}

{% block title %}Commander en ligne{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/header.css') }}">
    <link rel="stylesheet" href="{{ asset('css/place_order/index.css') }}">
    <link rel="stylesheet" href="{{ asset('css/place_order/main.css') }}">
    <link rel="stylesheet" href="{{ asset('css/place_order/pizza.css') }}">
    <link rel="stylesheet" href="{{ asset('css/place_order/basket.css') }}">
    <link rel="stylesheet" href="{{ asset('css/place_order/footer.css') }}">
{% endblock stylesheets %}

{% block body %}
    {{ include('_header.html.twig', with_context=false) }}
    <main>
        <div class="container-pizzas">
            {% for pizza in pizzas %}
                {{ include('place_order/_pizza.html.twig', {pizza: pizza}, with_context=false) }}
            {% endfor %}
        </div>
        <div class="container-basket">
            <h2>MA COMMANDE</h2>
            <div class="container-ordered-pizzas"></div>
            <div class="container-total">
                <span>TOTAL</span>
                <span id="total-price">0.00 €</span>
            </div>
            <button id="place-order-btn" disabled>COMMANDER</button>
        </div>
    </main>
    <footer></footer>
    <script>
        const websiteURL = "{{ url }}";
    </script>
    <script src="{{ asset('js/place_order/utils.js') }}"></script>
    <script src="{{ asset('js/place_order/renderNotification.js') }}"></script>
    <script src="{{ asset('js/place_order/renderOrder.js') }}"></script>
    <script src="{{ asset('js/place_order/removePizza.js') }}"></script>
    <script src="{{ asset('js/place_order/addPizza.js') }}"></script>
    <script src="{{ asset('js/place_order/index.js') }}"></script>
{% endblock %}
