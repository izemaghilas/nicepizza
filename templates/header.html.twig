<header>
    <a href="{{ path('nice_pizza_home') }}" class="nice-pizza-logo">
        <img src="{{ asset('img/logo.png') }}" alt="logo">
        <span>Nice Pizza</span>
    </a>
    <a href="{{ path(pathName) }}" class="orders-dashboard">
        <button>{{ btnName }}</button>
    </a>
</header>